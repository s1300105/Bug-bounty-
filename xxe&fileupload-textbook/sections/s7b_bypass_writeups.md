## バイパスwriteup（英語・日本語）

本節では、PortSwigger Web Security Academyの「File upload vulnerabilities」学習パスを実際に攻略した4本のwriteup（英語2本・日本語2本）を通じて、ファイルアップロード検証バイパスの実装レベルの手口を横断的に整理する。個々のラボの操作手順（「Go to exploit serverをクリックし…」といった攻略ナビゲーション）はここでは扱わず、**なぜそのバイパスが技術的に成立するのか**という原理の解説に絞る。読者が本節を読めば、拡張子ブラックリスト、Content-Type検証、マジックバイト検証、パストラバーサル、NULLバイト、競合状態(レースコンディション)という6系統のバイパスが、それぞれサーバー実装のどの前提を裏切っているのかを説明できるようになることを目標とする。

### 1. 拡張子ブラックリスト方式はなぜ突破されるのか（Apache `.htaccess`アップロード）

Aytxc氏のwriteupで扱われるラボ「Web shell upload via extension blacklist bypass」は、`.php`や`.php5`、`.phtml`といった典型的な実行可能拡張子だけを禁止リストに載せたアップロード機能を対象にしている。ここでの根本的な欠陥は、**ブラックリスト方式が「サーバーがどの拡張子を実行可能とみなすか」をアプリケーション側の想定でしか把握していない**という点にある。Apacheは`.php`以外にも、`AddType`や`AddHandler`ディレクティブをディレクトリ単位で上書きする`.htaccess`ファイル自体をアップロードディレクトリに置かせてしまえば、そこから先の拡張子解釈ルールを攻撃者が書き換えられる。

```apache
# 攻撃者がアップロードする .htaccess の中身
AddType application/x-httpd-php .l33t
```

この`.htaccess`を先にアップロードしておくと、以後そのディレクトリでは拡張子`.l33t`のファイルがPHPとして実行される。`.l33t`はブラックリストに存在しない拡張子なので、次に`exploit.l33t`のような名前でPHPコードをアップロードすればブラックリストを素通りし、Apache側では通常のPHPスクリプトとして解釈・実行される。

```php
<?php echo file_get_contents('/home/carlos/secret'); ?>
```

**原理**: ブラックリストは「既知の危険な拡張子を列挙して拒否する」という設計であり、原理的に「サーバーが実行可能とみなす拡張子の集合」を後から追加・変更できる仕組み（ここでは`.htaccess`によるハンドラ登録）がアップロード経路上に存在する限り、リストの網羅性は保証されない。これは典型的な**ブロックリスト（拒否リスト）方式の限界**であり、防御側は「許可リスト（アローリスト）方式」――許可する拡張子だけを列挙し、それ以外を一律拒否する設計へ転換する必要がある。加えて、`.htaccess`自体のアップロードやWeb公開ディレクトリ内でのApache設定ファイルの上書きを禁止することも合わせて必要になる。

> 出典: File upload vulnerabilities — Portswigger — https://medium.com/@aytxc/file-upload-vulnerabilities-portswigger-7cf5a42e0155

### 2. ポリグロットファイルによる画像検証の突破（ExifToolメタデータ注入）

同じくAytxc氏のwriteupが扱う、画像として有効なファイルであることをサーバーが検証するラボでは、**ポリグロットファイル**（複数のファイル形式として同時に有効なファイル）を使った突破が有効になる。ここでの手口は、正規のJPEG画像のメタデータ（コメントフィールド）にPHPコードを埋め込む方法である。

```bash
exiftool -Comment='<?php echo file_get_contents("/home/carlos/secret"); ?>' original.jpg -o polyglot.jpg
```

この`polyglot.jpg`は、先頭のマジックバイト(`FFD8FF`など、ファイル形式を識別するための先頭数バイトの固定シグネチャ)を含め正真正銘のJPEGとして成立しており、画像ビューアで開いても壊れた画像として表示されない。この状態のファイルを、アップロード先ではファイル名だけを`polyglot.php`のように拡張子`.php`に変更してアップロードする。サーバー側が「中身の一部がJPEGとして解釈できるか」という表層的な検証（例えばマジックバイトの先頭一致のみのチェック）しか行っていない場合、この検証をすり抜けたうえで、拡張子`.php`によってPHPインタプリタに渡され、埋め込まれたコメント内のPHPコードが実行される。

**原理**: 「ファイル形式の検証」と「拡張子による実行可否の決定」が別々のレイヤーで、かつそれぞれ独立に(部分的にしか)検証されていることが原因である。画像パーサはファイルの構造的整合性だけを見ており、コメントやEXIFメタデータのような「画像として無視してよい付随データ領域」までは意味的に検査しない。一方Webサーバー/PHPインタプリタは拡張子だけを見てPHPとして実行するかどうかを決める。両者の検証基準がズレているために、「画像としても有効」かつ「PHPとしても有効」という二重の性質を持つファイルが生成できてしまう。防御としては、画像を扱うなら再エンコード（サーバー側でGD/ImageMagick等を使って画像を再生成し、メタデータを含む元バイト列を破棄する）を行い、埋め込まれた任意コードごと無効化するのが有効な緩和策になる。

> 出典: File upload vulnerabilities — Portswigger — https://medium.com/@aytxc/file-upload-vulnerabilities-portswigger-7cf5a42e0155

### 3. 競合状態(レースコンディション)を利用したバイパス

Aytxc氏のwriteupではさらに、検証ロジック自体は堅牢だが、**「アップロードされたファイルがディスク上に書き込まれてから、検証が完了して不正なファイルが削除されるまでの間」に生じる時間差**を突くレースコンディションのラボにも触れている。多くのアップロード実装は次のような流れを取る。

1. アップロードされたファイルを一時的にWeb公開可能なディレクトリへ書き込む
2. ファイルの内容を検証する（拡張子、マジックバイト、ウイルススキャン等）
3. 検証に失敗した場合はファイルを削除する

この1〜3の間には必ず有限の処理時間が存在し、その間はファイルがディスク上に存在し、かつURLからアクセス可能な状態になっている。攻撃者はBurp Suiteの「Send group in parallel」（Turbo Intruderやリピーターのグループ送信機能を使い、複数のHTTPリクエストをほぼ同時にネットワークへ送出する仕組み）などを用いて、(a) 悪性PHPファイルのアップロードリクエストと、(b) アップロード先のファイルパスへのGETリクエストを同時に、多数回連射する。検証が完了する前のわずかな時間窓に(b)が命中すれば、削除される前のPHPファイルが実行されてしまう。

**原理**: これは「検証してから使う(Time-of-check to time-of-use, TOCTOU)」型の競合状態であり、検証と利用可能性の間にアトミック性（不可分性。途中状態を外部から観測・利用されない性質）が保証されていないことが根本原因である。防御としては、ファイルを最初から「Web公開ディレクトリの外側」かつ「ランダムで推測不可能な一時ファイル名」で受け取り、検証を通過したものだけを本番の配信可能な場所へ改めて移動する、という設計にすることで、そもそも未検証のファイルに外部からアクセスできる窓を作らないことが有効である。

> 出典: File upload vulnerabilities — Portswigger — https://medium.com/@aytxc/file-upload-vulnerabilities-portswigger-7cf5a42e0155

### 4. Content-Typeヘッダー偽装とマジックバイト検証のバイパス

ethichooligan（Rza Shirinov）氏のwriteupは、より広い範囲のPortSwiggerファイルアップロードラボ群を横断的に扱っており、Content-Type検証とマジックバイト検証それぞれの弱点を具体的に示している。

**Content-Typeバイパス**: サーバーが`multipart/form-data`リクエスト中の`Content-Disposition`ヘッダーが申告する`Content-Type`（例えば`image/jpeg`）だけを見てファイル種別を判定している場合、Burp Suiteのインターセプト機能でリクエストを書き換え、拡張子は`.php`のままヘッダー値だけを偽装すればよい。

```http
Content-Disposition: form-data; name="file"; filename="exploit.php"
Content-Type: image/jpeg

<?php echo file_get_contents('/home/carlos/secret'); ?>
```

このバイパスが成立するのは、**Content-Typeヘッダーがクライアント（ブラウザやリクエスト送信ツール）が任意に設定できる自己申告値にすぎない**ためである。サーバー側の検証コードが「クライアントの言い分」をそのまま信頼している時点で、これは認証・検証のsink（入力が最終的に評価・信頼される危険な代入先）として機能しなくなっている。信頼できるのはサーバー自身がファイルの実バイト列を読んで判定した結果だけであり、ヘッダー値は本質的に無検証の外部入力と同列に扱うべきである。

**マジックバイト検証バイパス**: これを踏まえ、多くの実装は「ファイル冒頭数バイトの固定シグネチャ（マジックバイト、例: JPEGなら`FF D8 FF`）」を見てファイル種別を判定するよう改善する。しかしこれも、**ファイル冒頭さえ正しい値であれば、それ以降のバイト列は自由**という性質上、正しいマジックバイトの直後にPHPコードを連結したファイルを作れば通過してしまう。

```
FF D8 FF ... (正規のJPEGヘッダー)
<?php system($_GET['cmd']); ?>
```

これは前述のポリグロット手法と本質的に同じ原理であり、「ファイルの一部だけを検証する」設計は常に「検証されない残りの部分に任意の内容を仕込める」という裏返しの脆弱性を持つ。

> 出典: PortSwigger — File upload vulnerabilities — https://ethichooligan.medium.com/portswigger-file-upload-vulnerabilities-51b09692c953

### 5. アーカイブ展開によるパストラバーサル

ethichooligan氏のwriteupはさらに、ZIPやTARなどのアーカイブファイルのアップロードを受け付け、サーバー側で展開する機能を狙ったパストラバーサル攻撃にも言及している。アーカイブ内のファイルエントリ名に`../`を含めておくことで、展開処理が意図したアップロード先ディレクトリの外側にファイルを書き出してしまう、いわゆる**Zip Slip**系の脆弱性である。

```
malicious.zip の中身（エントリ名）:
  ../../../var/www/html/shell.php
```

**原理**: アーカイブ展開ライブラリが、エントリ名をそのままファイルシステムのパスとして結合し、結合後のパスがベースディレクトリの範囲内に収まっているかを正規化・検証していない場合に成立する。これはXXE（XML外部実体参照）やその他のパストラバーサル系脆弱性と共通する原理――「外部から与えられた相対パス文字列を、境界チェックなしにファイルシステムAPIへそのまま渡している」――であり、本教科書の他章で扱うパストラバーサル対策（正規化後のパスがベースディレクトリのプレフィックスと一致するかを検証する等）がそのまま防御に応用できる。

> 出典: PortSwigger — File upload vulnerabilities — https://ethichooligan.medium.com/portswigger-file-upload-vulnerabilities-51b09692c953

### 6. NULLバイト注入による拡張子偽装（日本語writeup: Qiita）

Qiitaの日本語writeup「Web shell upload via obfuscated file extension #BSCP」は、NULLバイト(`%00`、値としてはバイト`0x00`)を使ったファイル名偽装の原理を、パーサの実装レイヤーの違いという観点から明快に解説している。

```
アップロード時のファイル名パラメータ:
exploit.php%00.jpg
```

このファイル名をURLデコードすると、内部的には`exploit.php\0.jpg`というバイト列になる。ここでバイパスが成立する理由は、**サーバー内の異なるコンポーネントが同じバイト列を異なる規則で「文字列の終端」を解釈する**という、レイヤー間の解釈不一致にある。

- **検証層（アプリケーションの拡張子チェックロジック）**: PHPやJavaなどの高水準言語の文字列処理関数の多くは、`\0`をただの1バイトとして扱い、文字列全体を最後まで走査する。そのため`exploit.php\0.jpg`は末尾が`.jpg`である文字列として認識され、画像拡張子として検証を通過する。
- **保存・実行層（OSのファイルシステムAPIなど、C言語ベースの低水準実装）**: C言語の文字列は伝統的にNULL終端方式（`\0`が来た時点でそこを文字列の終わりとみなす）を採用している。そのためファイルを実際にディスクへ書き込む段階のシステムコールでは、`exploit.php\0.jpg`は`\0`の時点で切り詰められ、`exploit.php`というファイル名で保存される。

結果として、「検証では`.jpg`に見え、保存時には`.php`になる」という矛盾したファイルが作成され、以後は通常のPHPファイルとしてWebサーバーから実行可能になる。Qiitaのwriteupが示すペイロード例は次のようなものである。

```php
<?php echo file_get_contents('/home/carlos/secret'); ?>
```

**バージョン依存性に関する重要な補足**: このNULLバイト系のバイパスはPHP 5.3.4以前で特に顕著だった。PHP 5.3.4（2010年〜2011年にリリース）以降、PHPコア自体のファイル操作関数（`fopen`等）でNULLバイトを含む文字列が渡された場合にエラーとして扱う修正が入っており、PHP単体の脆弱性としては大部分が塞がれている。しかし、(a) アプリケーション側の検証ロジックが独自にNULLバイトを考慮せず文字列比較を行っている場合や、(b) PHP以外の言語・フレームワーク、あるいはPHPからさらに外部コマンドやライブラリへファイル名を渡す経路がある場合には、依然として同種の解釈不一致が発生しうる。したがって「PHP最新版だから無関係」と即断せず、検証コードが拡張子の判定に用いている具体的な文字列比較の実装（`endsWith`相当の処理がNULLバイトをどう扱うか）を必ず確認する必要がある。

> 出典: Web shell upload via obfuscated file extension #BSCP — Qiita — https://qiita.com/yna020311/items/bed8a913452bed0c898a

### 7. 防御設計の統合的な整理（日本語記事: CyberCrew/spool）

最後に、CyberCrewの日本語記事は攻略writeupではなく、上記のような個別バイパス手法を踏まえたうえでの**防御側の設計原則**をまとめており、本節の総括として有用である。記事はファイルアップロード脆弱性の本質を「単なる実装ミスではなく、設計上の前提が一段ずれている状態」――すなわち「ファイルをどう解釈するか」という後工程の扱いにこそリスクが集中する、と整理している。

危険な実装の積み重ねとして次を挙げている。

- 拡張子のみで許可・不許可を判定している(ブラックリスト/ホワイトリストいずれであっても拡張子文字列比較だけに依存)
- ブラウザが申告するContent-Typeを無条件に信頼している
- アップロード先をWeb公開ディレクトリ直下にしている
- 保存したファイルに実行権限を付与している(あるいは実行権限を剥奪していない)

これに対する防御を、記事は次の「三点集約モデル」として整理している。

1. **中身検証**: マジックバイトや実際のファイル構造解析によって、拡張子や自己申告のContent-Typeではなくファイルの実体を判定する（ただし本節5・6で見た通り、単純な先頭バイトの一致だけでは不十分であり、必要なら画像の再エンコードのような踏み込んだ無害化が必要）
2. **保存分離**: アップロードされたファイルをWeb公開ディレクトリの外側に保存し、直接URLでアクセスできないようにする
3. **権限最小化**: 保存先ディレクトリ・ファイルに実行権限を与えず、かつファイル名をアプリケーション側でランダムに生成し直すことで、攻撃者が意図した名前・パスでのアクセスを困難にする

配信時についても、直接パスを返すのではなく、認証・認可を経たアプリケーション経由でのストリーミング配信、または有効期限付きの署名URLを用いることを推奨している。また運用面の監視ポイントとして、申告Content-Typeと実ファイルの不一致検知、不自然に複数のドットを含むファイル名、Web公開ディレクトリ配下への新規ファイル出現、Webサーバープロセスからの異常な外部通信、を継続的に監視することを挙げている。

**原理としてのまとめ**: 本節で見た6系統のバイパス（`.htaccess`によるハンドラ上書き、ポリグロット、競合状態、Content-Type偽装、マジックバイト部分検証の回避、NULLバイトによる解釈不一致、アーカイブ展開のパストラバーサル）は、いずれも「検証ロジックが参照している情報（拡張子文字列、自己申告ヘッダー、ファイル冒頭数バイト、設定ファイルが存在しない前提、検証と利用の間に時間差がない前提）」と、「実際にファイルを実行・解釈するコンポーネントが参照している情報」との間に**ズレ**があることに起因している。したがって防御の本質は個々のバイパス手法への対症療法ではなく、アップロードされたファイルを「保存」「検証」「配信・実行」という工程ごとに権限とアクセス経路を分離し、どの工程でも外部入力(ファイル内容・ファイル名・ヘッダー)を信頼しない設計に一元化することにある。

> 出典: ファイルアップロード機能に潜む脆弱性 — CyberCrew — https://cyber.spool.co.jp/unrestricted-file-upload-extension-bypass/
